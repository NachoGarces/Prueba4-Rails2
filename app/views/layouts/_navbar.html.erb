<nav class="navbar navbar-expand-lg navbar-dark bg-light fixed-top">
  <% if user_signed_in? %> <%= image_tag current_user.avatar, class:'w-90' %> <% end %>
  <a class="navbar-brand" href="<%= root_path %>">Bucket List del 18°</a>
    <div class="" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="<%= tasksusers_path %>">Completadas <%=  %>/<%= Task.count %> <span class="sr-only">(current)</span></a>
        </li>
        <% if user_signed_in? %>          
          <li class="nav-item active">
            <a class="nav-link" href="<%= destroy_user_session_path(current_user) %>" data-method="DELETE">Salir: <%= current_user.email%></a>
          </li>
        <% else %> 
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_session_path %>">Ingresar</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_registration_path %>">Registrar</a>
          </li>
        <% end %>
      </ul>
    </div>
</nav>