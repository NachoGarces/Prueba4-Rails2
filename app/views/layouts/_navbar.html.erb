<nav class="navbar navbar-expand-lg n-70 navbar-dark bg-light fixed-top">
  <% if user_signed_in? %> <%= image_tag current_user.avatar, class:'w-90 mr-3' %> <% end %>
  <a class="navbar-brand" href="<%= root_path %>">Lista del 18°</a>
  <span class="navbar-text">
    Completadas: <%= @tas %>/<%= Task.count %>
  </span>
    <div class="" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>          
          <li class="nav-item active">
            <a class="nav-link" href="<%= destroy_user_session_path(current_user) %>" data-method="DELETE">Salir: <%= current_user.email%></a>
          </li>
        <% else %> 
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_session_path %>">Ingresar</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_registration_path %>">Registrar</a>
          </li>
        <% end %>
      </ul>
    </div>
</nav>